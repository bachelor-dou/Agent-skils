"""store —— 数据层,三份数据共用一个写原语。

    atomic.py      唯一读写原语:排他锁 → 读 → 改 → 原子替换。读不出来就抛,绝不覆盖
    universe.py    Github_DB.json:观测宇宙 + 每个仓库的元信息
    snapshots.py   data/snapshots/*.json.gz:每日 star,全部增长计算的唯一来源
    favorites.py   data/favorites.json:用户收藏

三条贯穿整层的规则:

1. **读不出来就抛(`StoreReadError`),绝不写。** 把读失败当成 `{}` 再照写,一次 JSON
   截断就清空 5 万条记录。这件事只由 `atomic` 一处决定。
2. **每个字段只有一个写入者。** 见 `universe` 的字段归属表 —— 那是代码里的强制检查,
   写不属于自己的字段直接抛,不是注释约定。
3. **DB 损坏要抛,快照损坏按缺失处理。** 可替代性不同:锚点能顺延到邻近那天,DB 不能。

分工:快照是「某一天 × 全部仓库的 star」,按天一个 gz,取 T−7 只读 0.8MB;DB 是「全部仓库
× 元信息」。反过来把 `{日期: star}` 挂到项目下,实测主库 30MB→83MB、每次保存 0.8s→2.7s
(全程持锁)。
"""

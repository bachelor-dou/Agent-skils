# AGENTS.md

## 安装速查知识库（skills/）

`skills/` 是用户**个人的、自进化的**安装/用法速查知识库：每个子目录一个工具模块，
记录**用户实测跑通**的安装命令、用法、注意事项、踩坑解法。它不是泛化的通用知识——
查到什么就按什么执行。

**任何平台的 agent，凡涉及「安装/配置/使用某工具、搭建环境、回忆以前怎么弄的」：**

1. 先读 `skills/install-kb/SKILL.md`（总指导：定位、查询流程、模块模板、回写规约）；
2. 再定位 `skills/<模块>/SKILL.md`，严格按记录执行，不自行发挥、不给未验证的泛化方案；
3. 库里没有或要改库 → 现场解决 → **先问是否写入 `skills/`，同意后再按模板收录/回写**；没答应就不写，内容留在对话里。

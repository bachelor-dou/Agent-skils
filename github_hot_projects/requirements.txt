# github_hot_projects 依赖列表
# ====================================
# 核心依赖（管道模式 + Agent CLI 必须）
requests>=2.28.0

# API 服务依赖（仅 api_server 模式需要）
fastapi>=0.100.0
pydantic>=2.9.0
uvicorn>=0.20.0
markdown>=3.5.0
